language: c
compiler:
  - gcc
  - clang

before_install:
  - if [ $TRAVIS_OS_NAME == linux ]; then sudo apt-get update && sudo apt-get install -y tup; fi
  - if [ $TRAVIS_OS_NAME == osx ]; then brew update && brew install tup; fi

before_script:
  - tup init

script: tup 

os:
  #  - linux
  - osx
